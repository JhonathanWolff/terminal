unbind r
bind r source-file ~/.tmux.conf

set -g prefix C-q

set -g mouse on
set -g default-terminal "screen-256color" 
set-option -sa terminal-overrides ",xterm-256color:RGB"
set -g default-terminal "tmux-256color"

set -g status on

bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"
bind g display-popup -E -d "#{pane_current_path}" -w "90%" -h "90%" -T "Git Status" "lazygit"

bind x kill-pane

bind b previous-window

set-option -g status-position bottom
set -g status-style bg=default

# Catppuccin Plugin Setup
set -g @catppuccin_flavor 'mocha'
set -g @catppuccin_window_status_style "rounded"
run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux

# General Tmux Settings
set -g status-right-length 100
set -g status-left-length 100
set -g status-left "" 

setw -g mode-keys vi
set -g status-keys vi
set -s escape-time 50 # delay on scape character


# Default Status Bar
set -g status-right "#{E:@catppuccin_status_application}#{E:@catppuccin_status_session}"
#set -ag status-right "#{E:@catppuccin_status_uptime}"

# Custom status bar modules (define your variables)

# Hora atual
set -ag status-right "#[bg=#{@thm_yellow},fg=#{@thm_crust}]#[reverse]#[noreverse]  "
set -ag status-right "#[fg=#{@thm_fg},bg=#{@thm_mantle}] #(date '+%d/%m %H:%M') "


# #Temperature 

#need to configure a cron
# run 
#mkdir -p $HOME/cronjobs && echo "curl -s \"wttr.in/?format=%c+%t+(%f)+%p+%h+%w&period=60\" > $HOME/wheater.txt" > $HOME/cronjobs/wheater.bash && sudo chmod +x $HOME/cronjobs/wheater.bash

#configure cron job
#crontab -e
# */5 * * * * $HOME/cronjobs/wheater.bash

set -ag status-right "#[bg=#{@thm_flamingo},fg=#{@thm_crust}]#[reverse]#[noreverse] "
set -ag status-right "#[fg=#{@thm_fg},bg=#{@thm_mantle}] #(cat $HOME/wheater.txt) "


# TPM Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Last line execution
run "$HOME/.config/tmux/plugins/tpm/tpm"

